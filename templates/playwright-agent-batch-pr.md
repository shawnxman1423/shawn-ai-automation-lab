# PR: Playwright agent batch

## Summary

This PR adds a small Playwright E2E test batch generated with AI assistance and manually kept as normal Playwright code.

## Stacking note

This PR is stacked on top of the existing Playwright-agent parent branch:

```text
base: poc/playwright-agents
```

Retarget this PR to `staging` only after the parent PR has merged, unless the team chooses a different integration flow.

## Coverage

- Area:
- Specs added/updated:
- Existing page objects/fixtures reused:

## Verification

Commands run:

```bash
# fill in exact commands
```

Result:

```text
# fill in output summary
```

## Safety

- [ ] No secrets committed
- [ ] No auth state committed
- [ ] No traces/screenshots/videos committed
- [ ] Tests use existing fixtures/page objects where possible
- [ ] CI runs normal Playwright code with no AI runtime dependency
